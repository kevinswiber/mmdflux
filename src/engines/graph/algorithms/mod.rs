//! Shared graph-family layout algorithms.
//!
//! Engine adapters live alongside this namespace under `engines::graph`.
//! Algorithms are grouped separately so contributors can distinguish
//! adapter policy from reusable layout kernels.

pub mod layered;
